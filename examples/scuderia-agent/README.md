# Scuderia dogfood example

Canonical live demo:

```bash
python -m pip install -e ../../packages/python
agent-ledger init --url https://YOUR-WORKER.workers.dev --api-key al_YOUR_KEY
agent-ledger doctor
agent-ledger dogfood-scuderia
```

See `../../docs/FIVE_MINUTE_QUICKSTART.md`.
