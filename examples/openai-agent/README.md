# openai-agent example

Configure `AGENT_LEDGER_URL` and `AGENT_LEDGER_API_KEY`, execute one consequential action, capture its `receipt_id`, then run `agent-ledger verify alr_...`. Replace the placeholder action with the framework/tool named by this directory.
